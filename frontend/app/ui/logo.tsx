import { newsreader } from "../fonts";

const Logo = () => {
  return (
    <div
      className={`text-blue-500 ${newsreader.className} font-bold md:text-[44px] text-3xl`}>
      MyMovies
    </div>
  );
};

export default Logo;
